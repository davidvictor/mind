---
description: Thin wrapper for the canonical mind dream surface
argument-hint: light|deep|rem|weave [--dry-run] | bootstrap [--dry-run] [--force-pass-d] [--checkpoint-every N] [--resume] [--limit N] | campaign --days N [--start-date YYYY-MM-DD] [--dry-run] [--resume] [--profile aggressive|yearly]
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind dream $ARGUMENTS
```

Use `light`, `deep`, `rem`, `weave`, `bootstrap`, or `campaign`. `light`, `deep`, `rem`, and `weave` support `--dry-run`. `bootstrap` also supports `--force-pass-d`, `--checkpoint-every`, `--resume`, and `--limit`. `campaign` supports `--days`, `--start-date`, `--dry-run`, `--resume`, and `--profile`.
