---
description: Thin wrapper for the canonical mind config surface
argument-hint: show|path
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind config $ARGUMENTS
```

Use `show` to inspect the resolved config and `path` to print the active config file.
