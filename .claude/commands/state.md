---
description: Thin wrapper for the canonical mind state surface
argument-hint: [health|queue|locks|runs|run <id>]
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind state $ARGUMENTS
```

Run without arguments for the summary view, or pass a subcommand for a focused report.
