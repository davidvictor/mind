---
description: Thin wrapper for the canonical mind lint command
argument-hint: [path] [-v]
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind lint $ARGUMENTS
```

Use `.venv/bin/python -m mind lint -v` for a more detailed report.
