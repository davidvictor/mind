---
description: Thin wrapper for the canonical mind expand command
argument-hint: <question>
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind expand "$ARGUMENTS"
```

Use `.venv/bin/python -m mind expand --help` for options like `--limit` and `--force-web`.
