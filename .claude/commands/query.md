---
description: Thin wrapper for the canonical mind query command
argument-hint: <question>
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind query "$ARGUMENTS"
```

Use `.venv/bin/python -m mind query --help` for options like `--limit`.
