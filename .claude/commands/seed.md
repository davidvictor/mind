---
description: Thin wrapper for the canonical starter-graph seed surface
argument-hint: --preset core|skeleton|framework
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind seed $ARGUMENTS
```

Use `--preset skeleton` for the recommended day-one starter graph. Use `core` for the lean backbone and `framework` for the broader connected starter layout.
