---
description: Thin wrapper for the canonical Obsidian theme surface
argument-hint: theme apply --dark dragon --light lotus
---

Run the canonical CLI:

```bash
.venv/bin/python -m mind obsidian $ARGUMENTS
```

Use `theme apply` to regenerate the repo-managed Kanagawa-based Obsidian design system, enable the managed CSS snippet, and rewrite the canonical graph color configuration.
