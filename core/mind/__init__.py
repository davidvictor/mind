"""Thin canonical CLI surface for the Brain runtime.

Phase 1 starts the `mind` package as the forward-compatible command entry
point while the bulk of the implementation still lives under `scripts/`.
"""
